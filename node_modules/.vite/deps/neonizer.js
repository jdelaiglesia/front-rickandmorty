import {
  require_react
} from "./chunk-ZVMIEU5R.js";
import {
  __toESM
} from "./chunk-UXIASGQL.js";

// node_modules/neonizer/index.js
var import_react = __toESM(require_react());
var Neonizer = ({
  children,
  className = "",
  time = 2e3,
  excludeColors = [],
  newColors = []
}) => {
  const [currentColor, setCurrentColor] = (0, import_react.useState)("");
  const baseColors = [
    "#1f51ff",
    // neon_blue
    "#0ff0fc",
    // electric_cyan
    "#bc13fe",
    // neon_purple
    "#8a2be2",
    // proton_purple
    "#ff44cc",
    // neon_pink
    "#ea00ff",
    // neon_magenta
    "#ff1493",
    // plastic_pink
    "#fff01f",
    // neon_yellow
    "#e7ee4f",
    // absinthe
    "#dfff00",
    // chartreuse_yellow
    "#ff3131",
    // neon_red
    "#ff5e00",
    // electric_orange
    "#39ff14",
    // neon_green
    "#7fff00",
    // ufo_green
    "#ccff00"
    // electric_lime
  ];
  const normalizeColorCode = (color) => {
    if (!color.startsWith("#")) {
      return `#${color}`;
    }
    return color;
  };
  const normalizeColors = (colors2) => {
    return colors2.map((color) => normalizeColorCode(color));
  };
  const filteredNewColors = newColors.filter((color) => color.trim() !== "");
  const filteredExcludeColors = excludeColors.filter(
    (color) => color.trim() !== ""
  );
  const colors = [
    ...normalizeColors(baseColors),
    ...normalizeColors(filteredNewColors)
  ];
  (0, import_react.useEffect)(() => {
    const interval = setInterval(() => {
      setCurrentColor(getRandomColor(colors, filteredExcludeColors));
    }, time);
    return () => clearInterval(interval);
  }, []);
  const getRandomColor = (colors2, exclude) => {
    let updatedColors = colors2.filter(
      (color) => !exclude.includes(normalizeColorCode(color))
    );
    return updatedColors[Math.floor(Math.random() * updatedColors.length)];
  };
  const neonStyle = {
    color: currentColor,
    transitionProperty: "all",
    transitionDuration: "500ms"
  };
  return import_react.default.createElement("span", { className: `${className}`, style: neonStyle }, children);
};
var neonizer_default = Neonizer;
export {
  neonizer_default as default
};
//# sourceMappingURL=neonizer.js.map
